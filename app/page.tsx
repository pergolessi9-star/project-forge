import { db } from "@/core/db";
export const dynamic="force-dynamic";
const stages=[["INTAKE","Idea capture"],["PROBLEM","Problem evidence"],["MARKET","Market discovery"],["TECHNOLOGY","Feasibility"],["REGULATION","Compliance"],["EVIDENCE","Verification"],["CONCEPT","Concept"],["MVP","MVP"],["VALIDATION","Validation"],["SPECIFICATION","Engineering handoff"]];
export default async function Dashboard(){try{const [projects,metrics,evidence,reviews]=await Promise.all([db`SELECT p.*,COUNT(e.id)::int AS evidence_count FROM projects p LEFT JOIN evidence e ON e.project_id=p.id GROUP BY p.id ORDER BY p.updated_at DESC`,db`SELECT COUNT(*)::int AS projects,COUNT(*) FILTER(WHERE status IN ('DISCOVERY','EVIDENCE_REVIEW'))::int AS discovery FROM projects`,db`SELECT COUNT(*)::int AS total FROM evidence`,db`SELECT COUNT(*)::int AS total FROM human_reviews`]);return <div className="shell"><header className="topbar"><div className="brand">PROJECT FORGE</div><div className="version">DISCOVERY ENGINE · v0.1.0</div></header><main className="main"><section className="hero"><div><div className="eyebrow">Evidence before engineering</div><h1>Discovery Command Center</h1><div className="subtitle">Transform ideas into evidence-backed project concepts, validate them, and only then generate the engineering package.</div></div><a className="button" href="/api/projects">Projects API</a></section><section className="grid"><div className="card"><div className="label">Projects</div><div className="metric">{metrics[0]?.projects??0}</div></div><div className="card"><div className="label">In Discovery</div><div className="metric">{metrics[0]?.discovery??0}</div></div><div className="card"><div className="label">Evidence Items</div><div className="metric">{evidence[0]?.total??0}</div></div><div className="card"><div className="label">Human Reviews</div><div className="metric">{reviews[0]?.total??0}</div></div></section><section className="section"><h2>Discovery Pipeline</h2><div className="pipeline">{stages.map(([name,label])=><div className="stage" key={name}><strong>{name}</strong><small>{label}</small></div>)}</div></section><section className="section"><h2>Projects</h2><table className="table"><thead><tr><th>Code</th><th>Project</th><th>Stage</th><th>Status</th><th>Evidence</th></tr></thead><tbody>{projects.map((p:any)=><tr key={p.id}><td>{p.code}</td><td><a href={`/projects/${p.id}`}><strong>{p.name}</strong></a></td><td><span className="badge">{p.current_stage}</span></td><td>{p.status}</td><td>{p.evidence_count}</td></tr>)}</tbody></table></section><section className="section"><div className="card"><div className="eyebrow">Governance boundary</div><h2>AI output is not automatically verified evidence.</h2><p className="subtitle">Every material claim must be traceable to a source and, where required, pass human verification before it becomes accepted project knowledge.</p></div></section></main></div>}catch{return <div className="shell"><header className="topbar"><div className="brand">PROJECT FORGE</div><div className="version">DATABASE NOT CONNECTED</div></header><main className="main"><section className="hero"><div><div className="eyebrow">Infrastructure gate</div><h1>PostgreSQL connection required</h1><p className="subtitle">Configure DATABASE_URL and run the canonical migration and seed before opening the operational dashboard.</p></div><a className="button" href="/api/health">Health check</a></section></main></div>}}

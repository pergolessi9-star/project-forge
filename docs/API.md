# API

## Health
`GET /api/health`

## Projects
`GET /api/projects`
`POST /api/projects`

## Evidence
`GET /api/evidence`

The API layer is deliberately separated from domain services so discovery workflows can expand without coupling the UI to persistence details.
